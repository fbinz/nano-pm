node_modules/
test-results/
playwright-report/
.playwright/
e2e/test-results/
e2e/playwright-report/
*.log
.DS_Store

# Python / Django / uv
.venv/
__pycache__/
*.pyc
*.pyo
db.sqlite3
db.sqlite3-journal
.python-version

# Tailwind standalone CLI + DaisyUI plugin (fetched by `just css-deps`).
# The built `src/static/css/app.css` IS committed — keeps the Dockerfile
# Python-only (consistent with vendored JS under static/vendor/).
bin/tailwindcss
bin/daisyui.js
bin/daisyui-theme.js

# Local collectstatic output (regenerated; never commit)
staticfiles/
